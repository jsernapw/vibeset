{
  "name": "vibeset",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=22.10"
  },
  "volta": {
    "node": "22.21.0"
  },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "verify": "turbo run build typecheck test --concurrency=2",
    "dev": "turbo run dev",
    "lint": "oxlint .",
    "format": "prettier --write .",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.0",
    "typescript": "^5.7.0",
    "oxlint": "^0.15.0",
    "prettier": "^3.4.0",
    "vitest": "^3.0.0",
    "@vitest/coverage-v8": "^3.0.0"
  }
}
